version: '3'

services:
  teledeceptor:
    build: .
    ports:
      - "80:5000"
    environment:
      - API_ID=${API_ID}
      - API_HASH=${API_HASH}
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_ID=${ADMIN_ID}
    restart: unless-stopped 